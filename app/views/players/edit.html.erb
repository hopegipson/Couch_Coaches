<%= render 'partial' %>


<center>
<%= form_for @player do |f|%>
    <%= f.label "Select a team to add this player to:"%>
    <%= f.collection_select :team_id,  @current_user.teams.all_except(@free_agent_team), :id, :name, :include_blank => true %>
    <%= f.submit "Add Player" %>
<% end %>
